export type EventType = "MilestoneReleased" | "MilestoneArbitrated" | "ArbitrationForced" | "FundsReclaimed";
export type SettlementStatus = "PENDING" | "RETRYING" | "AUTHORIZED" | "MINTING" | "MINTED" | "FAILED" | "PERMANENT_FAILURE";
export interface Settlement { settlementKey: string; escrowAddress: string; eventType: EventType; milestoneIndex: number | null; recipient: string; amount: string; txHash: string; blockNumber: number; logIndex: number | null; status: SettlementStatus; attemptCount: number; gatewayTransferId?: string | null; mintTxHash?: string | null; }
export async function getSettlements(base: string, escrowAddress?: string): Promise<Settlement[]> { const response = await fetch(`${base}/transfers`); if (!response.ok) throw new Error("Settlement service unavailable"); const rows = await response.json() as Settlement[]; return escrowAddress ? rows.filter(row => row.escrowAddress?.toLowerCase() === escrowAddress.toLowerCase()) : rows; }
export async function getSettlement(base: string, key: string): Promise<Settlement> { const response = await fetch(`${base}/transfers/${encodeURIComponent(key)}`); if (!response.ok) throw new Error("Settlement not found"); return response.json() as Promise<Settlement>; }
